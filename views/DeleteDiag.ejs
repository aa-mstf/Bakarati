<%- include('partials/header'); -%>

<div>
  <form action="/api/removeDiag">
    <label for="ID">Diagnosis ID:</label>

    <input type="number" id="ID" name="id of the Diagnosis"
           value=0>
  
    <button>Delete Diagnosis</button>
  </form>
</div>


<%- include('partials/footer'); -%>

<script>
  const form= document.querySelector('form');
  const ID = document.querySelector('#ID');

  form.addEventListener('submit', async (e) => {
    e.preventDefault();
    const Id={
      "elementID":parseInt(ID.value)
    }
    console.log(Id)
    try {
      const res = await fetch('/api/removeDiag', { 
        method: 'POST', 
        body: JSON.stringify(Id),
        headers: {'Content-Type': 'application/json'}
      });
      location.assign('/diagPage');
    }
      catch (err) {
      console.log(err);
    }

  });
</script>