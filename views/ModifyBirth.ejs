<%- include('partials/header'); -%>

<div>
  <form action="/api/modifyBirth">
    <label for="ID">Birth ID:</label>

    <input type="number" id="ID" name="id of the cow"
           value=0>
    <label for="date">New Date:</label>

    <input type="date" id="date" name="date-of-addition"
                  value="2023-01-30"
                  min="2018-01-01" max="2023-12-31">
       
           <label for="Breed">New motherID:</label>
           <input type="number" id="motherID" name="id of the cow"
           value=0>
    <button>Modify Birth</button>
  </form>
</div>


<%- include('partials/footer'); -%>

<script>
  const form= document.querySelector('form');
  const ID = document.querySelector('#ID');
  const date = document.querySelector('#date');
  const motherID=document.querySelector('#motherID');

  form.addEventListener('submit', async (e) => {
    e.preventDefault();
    const Id={
      "elementID":parseInt(ID.value),
      "date":date.value,
      "motherId":motherID.value
    }
    console.log(Id)
    try {
      const res = await fetch('/api/modifyBirth', { 
        method: 'POST', 
        body: JSON.stringify(Id),
        headers: {'Content-Type': 'application/json'}
      });
      location.assign('/birthPage');
    }
      catch (err) {
      console.log(err);
    }

  });
</script>